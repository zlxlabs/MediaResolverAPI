services:
  media-resolver:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8100:8000"
    env_file:
      - ../.env
    volumes:
      - ../data:/app/data
      - ../logs:/app/logs
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
