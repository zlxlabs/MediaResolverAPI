# @generated by docker-package skill
# @skill-version 2
services:
  media-resolver:
    image: media-resolver-api:latest
    container_name: media-resolver-api
    ports:
      - "8206:8000"
    env_file:
      - ../.env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
