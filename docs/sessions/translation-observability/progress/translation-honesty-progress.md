## 里程碑 1：设计稿入库

- 当前阶段：implementing
- 本段结论：已将任务卡指定的设计稿原样入库。工作树位于 delegate 分配的分支，尚未修改业务实现。
- 关键决策与已否决方案：遵循设计稿的翻译诚实性、缓存、模型名单、熔断、健康口和启动探测不变式；无新增决策。
- 下一步唯一动作：新增翻译诚实性与缓存路径的红测试，并保留断言失败证据。

## 里程碑 2：诚实性与缓存红测试

- 当前阶段：implementing
- 本段结论：新增翻译失败结构化结果、失败不写原文缓存、缓存空译文补译回写三条测试。基线运行得到 3 个 `AssertionError`，没有导入或语法错误。
- 关键决策与已否决方案：测试将整单成功与翻译状态分离，并把缓存补译作为独立路径锁定；无新增决策。
- 下一步唯一动作：实现 `TranslationResult` 并接通 resolve/cache，使本组测试变绿。

## 里程碑 3：结构化翻译结果与诚实缓存

- 当前阶段：implementing
- 本段结论：`TranslationResult`/`TranslationStatus` 已接入翻译服务、解析响应和缓存路径。翻译失败时整单保持成功，响应译文与缓存均为空；缓存空译文会补译并回写；目标测试 3/3 通过。
- 关键决策与已否决方案：以 `status == ok` 作为唯一写入译文的条件；未引入额外存储或视频降级抽象。
- 下一步唯一动作：新增模型名单、熔断和告警的红测试，并验证断言失败而非导入错误。

## 里程碑 4：模型名单、熔断与告警红测试

- 当前阶段：implementing
- 本段结论：新增主模型失效后切备用、名单全灭后冷却内零调用且单次告警、以及独立 Sentry 告警出口测试。基线运行得到 3 个 `AssertionError`，未出现导入、属性或语法错误。
- 关键决策与已否决方案：只对明确的模型不存在/无渠道错误做模型冷却；全灭才打开翻译熔断，告警固定使用 `translation_upstream_unavailable`。
- 下一步唯一动作：实现模型名单、熔断冷却和可行动告警，使本组测试变绿。

## 里程碑 5：模型名单、熔断与告警

- 当前阶段：implementing
- 本段结论：翻译服务现在按主模型和逗号分隔备用模型顺序请求，明确的模型不存在/无可用渠道错误会进入冷却；名单全灭后打开实例级熔断并在冷却内零调用。告警通过固定指纹的 Sentry/GlitchTip 出口发送且上报失败不影响翻译结果，相关测试 4/4 通过。
- 关键决策与已否决方案：网络/超时/普通 500 不标记模型死亡；熔断状态只保存在解析层单例服务实例，不引入 Redis 或新存储。
- 下一步唯一动作：接入健康口、启动探测，并在测试夹具中默认关闭启动探测。

## 里程碑 6：翻译健康口与启动探测

- 当前阶段：implementing
- 本段结论：新增 `/health/translation`，只返回翻译状态和熔断标志；`/health` 始终保持存活语义。启动探测按运行时开关执行，失败打开翻译熔断但不阻止应用启动，测试夹具自动关闭真实探测；相关测试 4/4 通过。
- 关键决策与已否决方案：健康口不接 Docker HEALTHCHECK；探测异常由启动生命周期隔离并转换为翻译熔断，不冒泡成进程启动失败。
- 下一步唯一动作：补齐 README、`.env.example`、CHANGELOG，并核对响应字段与运维说明。
