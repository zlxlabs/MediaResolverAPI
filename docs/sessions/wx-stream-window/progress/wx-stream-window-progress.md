# wx-stream-window 进度

## 2026-09-02 提交① 配置与 `_read_window`

- 当前阶段：implementing
- 本段结论：新增 `STREAM_WINDOW_BYTES`（默认 4194304）与唯一打 CDN 的 `_read_window`：有界 Range、整块读完再 `aclose`、206 元数据校验。端点仍走旧透传，本段不改对外语义。
- 关键决策与已否决方案：轴 13 的「起点 0 + 200 + Content-Length ≤ 窗口」例外写在 `_read_window` 里，因为它封的是规范允许的行为。无。
## 2026-09-02 提交② 主循环替换透传

- 当前阶段：implementing
- 本段结论：端点改为有界窗口探针 + `_iter_windows` 拼接，客户端无 Range 仍是 200、有 Range 仍是 206。旧的「CDN 首包 200/206 矩阵」已删，改由窗口大小 × Range 矩阵锁字节正确性。
- 关键决策与已否决方案：中间窗口 Content-Range 短于请求且未到 EOF 视为短包失败，不再沿用旧的「按 CDN 终点截断」；通配 `*` 完整长度无法给出准确 Content-Length，首窗按 502 处理。
- 下一步唯一动作：给窗口失败加上 3 次/总预算 20 的重试、401/403/404/410 换对，以及后续窗断开语义。

## 2026-09-02 提交③ 重试 / 换对 / 200 例外

- 当前阶段：implementing
- 本段结论：单窗口最多 3 次、整请求预算 20；401/403/404/410 且起点 ≥ 131072 才向 TikHub 换一对 URL。后续窗口失败在响应头已发出后断连接，首窗耗尽仍是 502。
- 关键决策与已否决方案：起点 < 131072 的 403 立即失败、不重试同一条注定过期的 URL。无。
- 下一步唯一动作：给 `_iter_windows` 加预读一个窗口，并在取消 / GeneratorExit 时关掉预读任务及其 CDN 响应。

## 2026-09-02 提交④ 预读与取消清理

- 当前阶段：implementing
- 本段结论：消费当前窗口时后台预读下一个；客户端 `aclose` / 取消会取消预读任务并 `aclose` 其 CDN 响应，不留孤儿任务。
- 关键决策与已否决方案：无。
- 下一步唯一动作：改 README / `.env.example`，并写收尾进度。

## 2026-09-02 提交⑤ README 与环境变量

- 当前阶段：implementing
- 本段结论：文档去掉「服务端不会自动续拉 / 无 Range 收到不完整 206 返回 502」等旧语义，补上 `STREAM_WINDOW_BYTES`。
- 关键决策与已否决方案：无。
- 下一步唯一动作：红验三条不变式、全量 pytest、连续 5 次，以及本机 A/B/C 端到端。
