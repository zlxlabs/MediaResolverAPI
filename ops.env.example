# 可观测密钥 —— 部署机上复制为 ops.env(chmod 600),绝不进 git、绝不写入 .env。
# compose 以独立 env_file(required:false)注入，只进 os.environ、不被 pydantic/.env 枚举。
# DSN 来自 GlitchTip fordeal org 的 media-resolver-api project。
SENTRY_DSN=http://<public_key>@100.107.95.24:9000/<project_id>
